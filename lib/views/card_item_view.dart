import 'package:flutter/material.dart';

class CardItemView extends StatelessWidget {
  const CardItemView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('CardItemView'),
      ),
    );
  }
}
